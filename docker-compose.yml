version: "3.9"
services:
  mkdocs:
    # specifying the image here tags the image being built rather than pulls the image
    image: fyliu/mkdocs:testing-alpine
    build:
      context: .
      dockerfile: Dockerfile.alpine
    container_name: mkdocs
    command: mkdocs serve -a "0.0.0.0:8000"
    ports:
      - "8005:8000"
    volumes:
      - .:/app
